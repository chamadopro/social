═══════════════════════════════════════════════════════════════
  COMANDOS GIT - VERSÃO COMPLETA (COM TODOS OS ARQUIVOS)
═══════════════════════════════════════════════════════════════

⚠️ IMPORTANTE: Execute estes comandos na ordem exata!

═══════════════════════════════════════════════════════════════

1️⃣ INICIALIZAR GIT
───────────────────────────────────────────────────────────────
git init


2️⃣ RENOMEAR BRANCH PARA MAIN
───────────────────────────────────────────────────────────────
git branch -M main


3️⃣ ADICIONAR TODOS OS ARQUIVOS DO PROJETO
───────────────────────────────────────────────────────────────
git add .


4️⃣ FAZER O PRIMEIRO COMMIT (COM TODOS OS ARQUIVOS)
───────────────────────────────────────────────────────────────
git commit -m "feat: Implementação inicial do sistema ChamadoPro

- Sistema completo de chamados e serviços
- Painel administrativo completo
- Autenticação e autorização
- Sistema de pagamentos e escrow
- WebSocket para notificações em tempo real
- Documentação completa"


5️⃣ ADICIONAR REPOSITÓRIO REMOTO
───────────────────────────────────────────────────────────────
git remote add origin https://github.com/chamadopro/social.git


6️⃣ VERIFICAR SE ESTÁ CORRETO
───────────────────────────────────────────────────────────────
git remote -v


7️⃣ ENVIAR PARA O GITHUB
───────────────────────────────────────────────────────────────
git push -u origin main


═══════════════════════════════════════════════════════════════
  📝 DIFERENÇAS DA VERSÃO DO GITHUB
═══════════════════════════════════════════════════════════════

❌ NÃO FAÇA ISSO (versão do GitHub para repositório vazio):
   echo "# social" >> README.md
   git add README.md
   git commit -m "primeiro commit"

✅ FAÇA ISSO (versão completa para projeto existente):
   git add .
   git commit -m "feat: Implementação inicial do sistema ChamadoPro..."

═══════════════════════════════════════════════════════════════
  ⚠️ AUTENTICAÇÃO NO GITHUB
═══════════════════════════════════════════════════════════════

Quando executar "git push", você precisará autenticar:

1. O GitHub pedirá: Username e Password
2. Username: seu usuário do GitHub
3. Password: NÃO use sua senha! Use um PERSONAL ACCESS TOKEN

Como criar o token:
1. Acesse: https://github.com/settings/tokens
2. Clique em "Generate new token (classic)"
3. Nome: "ChamadoPro Local"
4. Marque: ✅ repo (Full control of private repositories)
5. Clique em "Generate token"
6. COPIE o token (você só verá uma vez!)
7. No terminal, quando pedir "Password:", cole o token

═══════════════════════════════════════════════════════════════
  🔍 VERIFICAR ANTES DE FAZER PUSH
═══════════════════════════════════════════════════════════════

Antes de fazer push, verifique o que será enviado:

git status

Isso mostrará todos os arquivos que serão commitados.
Certifique-se de que arquivos sensíveis (.env) NÃO estão na lista!

═══════════════════════════════════════════════════════════════
  🆘 PROBLEMAS COMUNS
═══════════════════════════════════════════════════════════════

❌ "remote origin already exists"
   Solução:
   git remote remove origin
   git remote add origin https://github.com/chamadopro/social.git

❌ "authentication failed"
   Solução: Use o TOKEN, não a senha do GitHub

❌ "failed to push some refs"
   Solução:
   git pull origin main --allow-unrelated-histories
   git push -u origin main

❌ "nothing to commit, working tree clean"
   Solução: Verifique se executou "git add ." antes do commit

═══════════════════════════════════════════════════════════════

